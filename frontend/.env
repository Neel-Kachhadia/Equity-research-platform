# Frontend environment variables
# Vite exposes VITE_* variables to the browser

# Backend API base URL (no trailing slash)
# Development: point at local FastAPI server
VITE_API_URL=http://localhost:8000
